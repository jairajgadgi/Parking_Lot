- name: Restart Application
  uses: appleboy/ssh-action@v1.0.3
  with:
    host: ${{ secrets.EC2_HOST }}
    username: ${{ secrets.EC2_USER }}
    key: ${{ secrets.EC2_SSH_KEY }}
    script: |
      sudo systemctl restart parkinglot
      sleep 10
      sudo systemctl status parkinglot --no-pager